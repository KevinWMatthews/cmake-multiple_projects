Create library.
    How to force library to check its public interface?
    Does it provides the header files that it must?
    Does it provide the functions that it must?

Add versions in each project, print them to console.

Make project depend on specific library version.
